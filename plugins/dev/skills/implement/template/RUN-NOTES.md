# Run Notes

Environment and process discoveries only — things a later task would otherwise rediscover the hard way (build quirks, failing tooling, workarounds, misleading
commands). One or two lines per entry, under a `## Task N` heading. Do not record implementation details or task summaries; those belong in the task report. Check for an existing entry before adding a duplicate.

<!-- entries below -->
